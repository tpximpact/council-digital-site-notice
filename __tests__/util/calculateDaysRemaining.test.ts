import { calculateDaysRemaining } from "@/util/calculateDaysRemaining";

describe.skip("calculateDaysRemaining", () => {
  it.skip("should return the correct number of days remaining", () => {});
});
